# 雾霾探测系统项目报告

## 一、 系统概述
随着环境问题的日益突出，雾霾对人们健康的影响愈发严重。本系统旨在为用户提供一个便携、实时、美观的移动端雾霾探测与天气查询平台。通过集成定位、实时天气获取及空气质量指数（AQI）分析，帮助用户在出行前做出科学决策。

## 二、 核心功能
1.  **自动定位系统**：利用浏览器 Geolocation API 获取用户位置，并将定位城市实时同步至服务器端存储，确保数据的持久化与多端一致性。
2.  **动态天气监控**：集成实时天气详情，包括温度、湿度、天气状况等。
3.  **AQI 深度探测**：核心功能为雾霾指数（AQI）的动态显示。系统根据 AQI 数值自动变换 UI 颜色（绿色代表优，橙色代表中度，红色代表重度污染），并提供相应的健康防护建议。
4.  **趋势可视化**：通过折线图（Chart.js）展示未来 24 小时的温度与湿度趋势，辅助用户预判雾霾扩散情况。
5.  **跨端适配**：采用 HTML5 响应式设计，完美适配不同像素密度的手机屏幕。

## 三、 技术架构
### 1. 前端 (Client Side)
-   **HTML5 & Semantic Tags**: 构建结构化的页面，确保 SEO 与访问性。
-   **Vanilla CSS3 (Glassmorphism)**: 采用现代玻璃拟态设计，使用背景模糊（Backdrop-filter）和线性渐变打造高级感。
-   **JavaScript (ES6+)**: 处理定位逻辑、异步数据交互及 UI 状态更新。
-   **Chart.js**: 负责复杂数据的图形化展现。

### 2. 后端 (Server Side)
-   **Go (Golang)**: 使用标准库 `net/http` 搭建高性能 Web 服务器。
-   **并发安全**: 采用 `sync.Mutex` 确保多用户并发访问时的数据安全。
-   **RESTful APIs**: 提供 `/api/location` 和 `/api/weather` 接口，实现数据的服务器端保存。
-   **静态资源托管**: 直接托管 `public` 目录下的前端资源。

## 四、 关键技术实现
-   **定位持久化**：客户端获取地理位置后，立即触发 POST 请求将城市名发送至服务器，满足“将定位城市保存在服务器端”的要求。
-   **动态 UI 反馈**：根据实时获取的 AQI 数值，动态计算进度条长度及颜色属性，实现视觉上的“动态显示”。
-   **适配方案**：使用 `viewport` 元标签 and 相对单位（rem/vh/vw），确保系统在不同尺寸的手机上均能保持一致的视觉比例。

## 五、 使用说明
1.  在项目根目录运行 `go run main.go` 启动服务器。
2.  使用手机或浏览器访问 `http://localhost:3000`。
3.  点击刷新按钮可重新触发定位与数据同步。
